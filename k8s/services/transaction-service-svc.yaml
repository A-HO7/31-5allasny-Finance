apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: finance-tracker
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
    - port: 8080
      targetPort: 8080
