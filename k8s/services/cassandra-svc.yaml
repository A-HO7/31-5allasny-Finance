apiVersion: v1
kind: Service
metadata:
  name: cassandra
  namespace: finance-tracker
  labels:
    app: cassandra
spec:
  clusterIP: None
  selector:
    app: cassandra
  ports:
    - port: 9042
      targetPort: 9042
      name: cql
