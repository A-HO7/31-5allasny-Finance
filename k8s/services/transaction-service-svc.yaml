apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: finance-tracker
  labels:
    app: transaction-service
spec:
  type: ClusterIP
  selector:
    app: transaction-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
