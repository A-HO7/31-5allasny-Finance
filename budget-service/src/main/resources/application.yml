server:
  port: 8080

spring:
  application:
    name: budget-service
  datasource:
    url: jdbc:postgresql://budget-postgres:5432/financedb-budgets
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: 6379
      password: redispass
#    cassandra:
#      contact-points: ${CASSANDRA_HOST:localhost}
#      port: 9042
#      local-datacenter: ${CASSANDRA_DC:datacenter1}
#      keyspace-name: financeks
#      schema-action: CREATE_IF_NOT_EXISTS
  mongodb:
    uri: mongodb://root:rootpass@${MONGO_HOST:localhost}:27017/financemongo?authSource=admin&serverSelectionTimeoutMS=50&connectTimeoutMS=50&socketTimeoutMS=50
  cassandra:
    contact-points: ${CASSANDRA_HOST:localhost}
    port: 9042
    local-datacenter: ${CASSANDRA_DC:datacenter1}
    keyspace-name: financeks
    schema-action: CREATE_IF_NOT_EXISTS
    connection:
      connect-timeout: 5s
      init-query-timeout: 5s
    request:
      timeout: 50ms

  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: auto
        default-requeue-rejected: false
        retry:
          enabled: true
          initial-interval: 1000
          # max-attempts is deprecated, max-retries should be used instead
          max-retries: 3

jwt:
  secret: ${JWT_SECRET:ThisIsAFallbackSecretKeyThatIsAtLeast32BytesLongForLocalDev}
  expiration: 86400000 # 24 hours in ms

logging:
  level:
    org.springframework.cache: TRACE
    org.springframework.data.redis: TRACE

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: budget-service

feign:
  account-service:
    url: ${FEIGN_ACCOUNT_SERVICE_URL:http://account-service:8080}
  budget-service:
    url: ${FEIGN_BUDGET_SERVICE_URL:http://budget-service:8080}
  reporting-service:
    url: ${FEIGN_REPORTING_SERVICE_URL:http://reporting-service:8080}
  user-service:
    url: ${FEIGN_USER_SERVICE_URL:http://user-service:8080}
  transaction-service:
    url: ${FEIGN_TRANSACTION_SERVICE_URL:http://transaction-service:8080}