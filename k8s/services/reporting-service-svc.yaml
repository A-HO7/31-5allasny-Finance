apiVersion: v1
kind: Service
metadata:
  name: reporting-service
  namespace: financetracker
  labels:
    app: reporting-service
spec:
  type: ClusterIP
  selector:
    app: reporting-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
