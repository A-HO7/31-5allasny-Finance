apiVersion: v1
kind: Service
metadata:
  name: transaction-postgres
  namespace: financetracker
spec:
  clusterIP: None
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: transaction-postgres
