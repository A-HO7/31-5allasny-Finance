apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: finance-tracker
data:
  # S4-INFRA will add their dashboard references here
  dashboards.json: |
    {
      "apiVersion": 1,
      "providers": []
    }
