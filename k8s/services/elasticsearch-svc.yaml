apiVersion: v1
kind: Service
metadata:
  name: elasticsearch
  namespace: finance-tracker
  labels:
    app: elasticsearch
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: elasticsearch
  ports:
    - name: http
      port: 9200
      targetPort: 9200
      protocol: TCP
