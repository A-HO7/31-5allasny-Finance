apiVersion: v1
kind: Service
metadata:
  name: reporting-postgres
  namespace: finance-tracker
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: reporting-postgres
  clusterIP: None
